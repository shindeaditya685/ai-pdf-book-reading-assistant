[build]
  command = "next build"
  publish = ".next"

# Ensure the PDF worker file has the correct MIME type
[[headers]]
  for = "/pdf-worker/*"
  [headers.values]
    Content-Type = "application/javascript; charset=utf-8"
